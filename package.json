{
  "name": "restapinode",
  "version": "1.0.0",
  "description": "…or create a new repository on the command line echo \"# restApiNode\" >> README.md git init git add README.md git commit -m \"first commit\" git branch -M main git remote add origin https://github.com/mfcortes/restApiNode.git git push -u origin main …or push an existing repository from the command line git remote add origin https://github.com/mfcortes/restApiNode.git git branch -M main git push -u origin main",
  "main": "App.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./src/App.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^2.0.12"
  }
}
